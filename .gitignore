# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Secrets
.env
backend/.env

# ChromaDB pre-built and committed via Git LFS (chroma.sqlite3)
# backend/chroma_db/

# Node
node_modules/
frontend/node_modules/

# Vite build output
frontend/dist/

# OS
.DS_Store
Thumbs.db

# Service report corpus — lives in SharePoint, not git (3.9 GB, ~8.9k PDFs)
backend/docs/
# Chroma DB build artefact — exceeds GitHub 100 MB file limit; do not commit updates
backend/chroma_db/
